defaults:
  - model/encoder: noposplat
  - model/decoder: splatting_cuda

seed: 111123

resolution: 512

input: inputs/

checkpointing:
  load: pretrained/mixRe10kDl3dv_512x512.ckpt

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d_%H-%M-%S}
